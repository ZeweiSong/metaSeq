prefix: "test"
threads: 1
max_threads_per_script: 4
tmp: "./tmp"

samples:
  test_H: rawSeq/test_H
  test_T: rawSeq/test_T

# int cutoff of freq number for select beads; 0 means output all beads
beadSelect: 10
distCutoff: 0.04
